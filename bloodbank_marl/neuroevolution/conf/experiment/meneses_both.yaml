# @package _global_
defaults:
  - override /environment: meneses_perishable
  - override /policies/replenishment: multi_product_mlp
  - override /policies/issuing: multi_product_mlp
  
wandb:
  init:
    project: meneses_neuroevo_test

environment:
  num_rollouts: 20

evosax:
  num_generations: 100

  strategy:
    popsize: 50

test_evaluator:
  num_rollouts: 50