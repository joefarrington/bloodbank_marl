# @package _global_
defaults:
  - override /policies/replenishment: discrete_order_up_to_mlp

plot_policies: True

wandb:
  init:
    project: bbm_neuro_de_moor_perishable_m2_exp2_fit_issue

environment:
  env_kwargs:
    max_useful_life: 2
    lead_time: 1
    max_order_quantity: 10

policies:
  optimize:
    - 1 # issuing