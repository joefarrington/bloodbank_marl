# @package _global_

defaults:
  - override /evosax/strategy: simple_ga
  - override /policies/issuing: heuristic_oufo

wandb:
  init:
    project: de_moor_m5_exp2
    tags: 
      - fit_rep
      - simple_ga
      - marl_env

environment:
  env_kwargs:
    max_useful_life: 5

policies:
  optimize:
    - 0 # replenishment