# @package _global_

defaults:
  - override /policies/replenishment: multi_agent_single_product_mlp

wandb:
  init:
    project: de_moor_m5_exp2
    tags:
      - fit_both
      - open_es
      - raw_action

environment:
  env_kwargs:
    max_useful_life: 5

policies:
  optimize:
    - 0 # replenishment
    - 1 # issuing