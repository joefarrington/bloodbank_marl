# @package _global_

wandb:
  init:
    project: de_moor_m2_exp6
    tags:
      - fit_both
      - open_es
      - rep_pretrained_order_up_to

environment:
  env_kwargs:
    lead_time: 2

policies:
  optimize:
    - 0 # replenishment
    - 1 # issuing
  replenishment:
    model_kwargs:
      n_hidden: [64]
  pretrained:
    replenishment:
      enable: True
      checkpoint_id: 0
      checkpoint_dir: /home/joefarrington/CDT/bloodbank_marl/model_cpts/de_moor_perishable/pretrained_replenishment_order_up_to/m2/exp6
      checkpoint_manager:
        _target_: orbax.checkpoint.CheckpointManager
        directory: ${policies.pretrained.replenishment.checkpoint_dir}
        checkpointers:
          _target_: orbax.checkpoint.PyTreeCheckpointer
        options:
          _target_: orbax.checkpoint.CheckpointManagerOptions
          max_to_keep: 1
          create: True