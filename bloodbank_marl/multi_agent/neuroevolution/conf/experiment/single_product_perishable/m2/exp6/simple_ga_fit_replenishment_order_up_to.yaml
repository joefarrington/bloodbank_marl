# @package _global_

defaults:
  - override /evosax/strategy: simple_ga
  - override /policies/issuing: heuristic_oufo

wandb:
  init:
    project: de_moor_m2_exp6
    tags:
      - fit_rep
      - simple_ga
      - marl_env

environment:
  env_kwargs:
    lead_time: 2

policies:
  optimize:
    - 0 # replenishment