# @package _global_

defaults:
  - override /evosax/strategy: simple_ga

wandb:
  init:
    project: de_moor_m2_exp2
    tags:
      - fit_both
      - simple_ga

plot_policies: True

policies:
  optimize:
    - 0 # replenishment
    - 1 # issuing