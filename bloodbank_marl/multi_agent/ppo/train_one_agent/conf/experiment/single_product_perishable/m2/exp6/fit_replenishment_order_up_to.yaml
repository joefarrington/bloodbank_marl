# @package _global_

defaults:
  - override /policies/replenishment: multi_agent_stochastic_single_product_order_up_to_mlp
  - override /policies/issuing: heuristic_oufo

wandb:
  init:
    project: "de_moor_m2_exp6"
    tags:
      - "fit_rep"

training:
  policy_to_train: 0
  num_steps: ${training.replenishment.num_steps}
  total_timesteps: 1e6

environment:
  env_kwargs:
    lead_time: 2