# @package _global_

defaults:
  - override /evosax/strategy: simple_ga
  - override /policies/replenishment: single_agent_single_product_mlp

wandb:
  init:
    project: "de_moor_m2_exp6"
    tags:
      - fit_rep
      - simple_ga
      - raw_action

environment:
  env_kwargs:
    lead_time: 2