# @package _global_
wandb:
  init:
    project: "simple_two_perishable_input_comparisons"
    tags:
      - "new_type_env"
      - "fit_both"
      - "simple_ga"
      - "compare_demand"

evosax:
  strategy:
    elite_ratio: 0.5
    sigma_init: 0.07
    sigma_decay: 1.0

environment:
  env_params:
    poisson_demand_mean: 8
  env_kwargs:
    max_order_quantity: 17

policies:
  optimize:
    - 0
    - 1

evaluation:
  seed: 24601