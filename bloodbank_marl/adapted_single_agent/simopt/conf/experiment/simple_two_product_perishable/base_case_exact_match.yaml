# @package _global_

defaults:
  - override /policies/issuing: heuristic_exact_match

wandb:
  init:
    project: "simple_two_perishable_base"
    tags:
      - "new_type_env"
      - "fit_rep"
      - "simopt"
      - "exact_match"
