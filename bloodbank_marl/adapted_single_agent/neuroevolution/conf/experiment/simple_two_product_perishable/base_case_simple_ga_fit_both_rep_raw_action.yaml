# @package _global_
defaults:
  - override /policies/replenishment: single_agent_multi_product_mlp

wandb:
  init:
    project: "simple_two_perishable_base"
    tags:
      - "new_type_env"
      - "fit_both"
      - "simple_ga"

policies:
  optimize:
    - 0
    - 1