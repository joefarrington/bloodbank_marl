# @package _global_

defaults:
  - override /policies/replenishment: heuristic_order_up_to
  - override /environment: de_moor_perishable_gymnax
  - override /param_search: grid
  - _self_

policies:
  replenishment_policy_params:

param_search:
  sampler:
    num_rollouts: 4000

evaluation:
  test_evaluator:
    num_rollouts: 10000