# @package _global_

defaults:
  - override /policies/replenishment_policy_params: SLp
  - override /policies/issuing: heuristic_priority_match

exp_name: SLp

evaluation:
  test_evaluator:
    num_warmup_days: 0