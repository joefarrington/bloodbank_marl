# @package _global_

defaults:
  - override /policies/replenishment_policy_params: Mp
  - override /policies/issuing: heuristic_riority_match

exp_name: Mp

evaluation:
  test_evaluator:
    num_warmup_days: 0