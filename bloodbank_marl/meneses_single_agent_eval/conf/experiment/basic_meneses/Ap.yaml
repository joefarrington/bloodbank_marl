# @package _global_

defaults:
  - override /policies/replenishment_policy_params: Ap
  - override /policies/issuing: heuristic_priority_match

exp_name: Ap

evaluation:
  test_evaluator:
    num_warmup_days: 0
