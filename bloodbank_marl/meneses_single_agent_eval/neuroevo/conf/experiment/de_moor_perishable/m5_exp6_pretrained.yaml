# @package _global_

defaults:
  - override /policies/replenishment: single_product_mlp
  - override /environment: de_moor_perishable_gymnax

wandb:
  init:
    project: "de_moor_gymnax_neuro_m5_exp6_pretrained"

environment:
  env_kwargs:
    max_useful_life: 5
    lead_time: 2
policies:
  pretrained:
    enable: True
    checkpoint_id: 0
    checkpoint_dir: /home/joefarrington/CDT/bloodbank_marl/model_cpts/rep_cpts_m5_L2
    checkpoint_manager:
      _target_: orbax.checkpoint.CheckpointManager
      directory: ${policies.pretrained.checkpoint_dir}
      checkpointers:
        _target_: orbax.checkpoint.PyTreeCheckpointer
      options:
        _target_: orbax.checkpoint.CheckpointManagerOptions
        max_to_keep: 1
        create: True


