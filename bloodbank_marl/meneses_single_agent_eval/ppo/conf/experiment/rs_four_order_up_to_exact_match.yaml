# @package _global_

defaults:
  - override /environment: rs_gymnax_four
  - override /policies/replenishment: multi_product_stochastic_order_up_to_mlp
  - override /policies/issuing: heuristic_exact_match

wandb:
  init:
    project: "rs_four_gymnax_ppo_exact_match_alt_rula"

environment:
  env_kwargs:
    max_order_quantities: 50